<% for receiver in @receivers %>
  <div class="item_section">
    <h3><%= receiver.name %></h3>
    <p>
    <%= receiver.introduction %>
    </p>
    <% if authenticated %>
      <p><%= link_to "View", receiver_path(receiver) %></p>
    <% end %>
  </div>
<% end %>
